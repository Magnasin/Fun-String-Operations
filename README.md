# Fun-String-Operations
If implemented correctly, you can run the executable file by passing through the two function arguments a and b as command line arguments. The program will take input from keyboard (one line at a time), and print out the corresponding result after the replacement is done. 
